<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose owns all in-app styling; this theme only has to hand the window
         a dark background and let content draw edge to edge. -->
    <style name="Theme.Vixel" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/delicat_background</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
