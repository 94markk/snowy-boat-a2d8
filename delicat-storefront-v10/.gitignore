# dist/ is generated by build/build.mjs and is committed, because a WordPress
# plugin is installed as a zip and must work without a build step on the server.
# Nothing else here is generated.
node_modules/
